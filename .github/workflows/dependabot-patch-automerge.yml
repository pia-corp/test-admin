name: Dependabot Auto Merge to Main

on:
  pull_request:
    branches:
      - main  # main ブランチへの PR が作成されたとき
    types:
      - opened
      - synchronize
      - reopened

permissions:
  pull-requests: write
  contents: write

jobs:
  merge-to-main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create PR from dependabot to main
        if: github.actor == 'dependabot[bot]'
        uses: actions/github-script@v4
        with:
          script: |
            const { data: pullRequest } = await github.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: "Merge dependabot updates from dependabot to main",
              head: "dependabot",
              base: "main",
              body: "Automated PR to merge updates from dependabot to main."
            });
            return pullRequest.html_url;

      - name: Auto-approve PR
        if: github.actor == 'dependabot[bot]'
        uses: actions/github-script@v4
        with:
          script: |
            const pullRequest = context.payload.pull_request;
            await github.pulls.createReview({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pullRequest.number,
              event: 'APPROVE'
            });

      - name: Auto-merge PR
        if: github.actor == 'dependabot[bot]'
        uses: actions/github-script@v4
        with:
          script: |
            const pullRequest = context.payload.pull_request;
            await github.pulls.merge({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pullRequest.number,
              merge_method: 'squash'
            });
